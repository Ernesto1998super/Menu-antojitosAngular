import { FiltcategoriaPipe } from './filtcategoria.pipe';

describe('FiltcategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltcategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
